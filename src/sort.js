/**
 * Sort.js contains methods that has to do with sorting an array. 
 *
 * @author Leia Lindberg <ll224np@student.lnu.se>
 * @version 1.0.0
 */

// Will create methods:
// * sortByDate(data, order)
// * sortType(data, order)
// * sortByKey(data, key, order)
// * reverseArray(data)