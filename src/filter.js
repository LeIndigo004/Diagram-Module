/**
 * Filter.js contains implementation that has to do with filtering an array. 
 *
 * @author Leia Lindberg <ll224np@student.lnu.se>
 * @version 1.0.0
 */

// Will create methods:
// * filterByRange(data, key, min, max)
// * filterByKey(data, key, value)
// * filterBySubstring(data, search)
// * filterByBoolean(data, key, value)
// * filterByCriteria(data, criteria)